# Reglas operativas - Agent/MCP

- Leer siempre `docs/arquitectura.md` y `docs/development.md` antes de editar.
- Clasificar cambios como `agente`; agregar `backend` si se tocan tools.
- Implementar primero agente local; MCP es opcional.
- El agente responde con datos procesados y no recalcula arbitrariamente el score.
- Registrar tarea completada, tarea pendiente y porcentaje de progreso.
