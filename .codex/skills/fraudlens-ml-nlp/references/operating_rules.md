# Reglas operativas - ML/NLP

- Leer siempre `docs/arquitectura.md` y `docs/development.md` antes de editar.
- Clasificar cambios como `ml-nlp` y, si aplica, `backend`.
- El modelo apoya priorizacion; no confirma fraude.
- Mantener RandomForestClassifier, LogisticRegression fallback, IsolationForest opcional, TF-IDF y cosine similarity.
- Registrar tarea completada, tarea pendiente y porcentaje de progreso.
